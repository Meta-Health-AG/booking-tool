import { Skeleton } from '@/components/ui/skeleton';

function AppointmentCardSkeleton() {
  return (
    <Skeleton className="flex items-center justify-center py-4 px-3.5 rounded-md h-[52px]" />
  );
}

export default AppointmentCardSkeleton;
